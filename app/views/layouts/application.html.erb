<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="utf-8" />
    <title>AKA - Andrea Kubná Architekt</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <!--[if IE]>
    <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <!--[if lt IE 8]>
    <script src="http://ie7-js.googlecode.com/svn/version/2.1(beta4)/IE8.js"></script>
    <![endif]-->
    <%= csrf_meta_tags %>
</head>
<body>
    <header>
        <div id="logo">
            <% if current_page?(root_path) %>
            <strong>
                <%= image_tag("logo.png") %>
            </strong>
            <% else %>
                <%= link_to(root_path) do %>
                    <%= image_tag("logo.png") %>
                <% end %>
            <% end %>
        </div>
        <nav>
            <%= link_to_unless_current("projekty", projects_path) do %>
                <strong>projekty</strong>
            <% end %>
            /
            <%= link_to_unless_current("aktuality", items_path) do %>
                <strong>aktuality</strong>
            <% end %>
        </nav>
    </header>

    <div class="container">
        <%= yield %>
    </div>

    <footer>
        <p>&copy; Andrea Kubná</p>
    </footer>
</body>
</html>
